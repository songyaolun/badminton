# 创建空间口令：用于创建新空间时的鉴权（运维通过 SSH 修改本文件）
CREATE_PASSWORD=change-me-please

# 分享链接加密密钥：必须为 64 位 hex（32 字节）。可用 `openssl rand -hex 32` 生成
SHARE_SECRET=replace-with-64-hex-chars-replace-with-64-hex-chars-replace-pad-

# 时区（影响过期判定）
TZ=Asia/Shanghai

# 监听地址
HTTP_ADDR=0.0.0.0:8090

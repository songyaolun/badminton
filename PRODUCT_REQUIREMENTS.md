# 熟人约局系统 — 产品需求文档

## 1. 产品定位

面向熟人圈子的轻量级多活动约局工具。用户可以在同一个空间内发布羽毛球、篮球、剧本杀、桌游、饭局等活动，群成员通过分享链接查看活动、报名、取消报名和转发。

系统不做公开活动发现，不做陌生人社交，不做复杂账号体系。核心目标是替代微信群里反复刷屏、难以统计的约局信息。

## 2. 核心原则

- 熟人圈子优先：持有空间密钥或分享链接即可参与。
- 多活动统一模型：不同活动类型使用同一套活动与报名流程。
- 类型模板扩展：活动差异通过模板字段表达，不为每种活动单独做页面。
- 轻量可信：无需注册登录，通过空间密钥和取消码完成基础保护。
- 移动端优先：微信内置浏览器里要能顺畅发布、报名和分享。

## 3. 核心概念

### 空间

一个熟人圈子或微信群对应一个空间。空间通过密钥识别，不同空间数据隔离。

### 活动类型

活动类型定义活动的分类和专属字段，例如羽毛球、篮球、剧本杀、桌游、其他。类型可以由系统预置，也可以由后台扩展。

### 活动

一次具体约局。所有活动都有公共字段，例如标题、时间、地点、费用、人数上限、发布人、取消码和备注。不同活动类型可以附加专属字段。

### 报名

成员参与活动的记录。报名时填写昵称和取消码。取消报名时需要输入对应取消码。

### 取消码

轻量身份验证方式。活动发布人设置活动取消码，报名者设置报名取消码。系统只保存哈希值。

## 4. 用户角色

### 空间成员

持有空间密钥或活动分享链接的人。可以查看活动、发布活动、报名、取消自己的报名，也可以在提供活动取消码后修改或取消活动。

### 系统管理员

部署者或维护者。通过后台查看空间、活动、活动类型模板和异常数据。管理员不参与普通成员报名流程。

## 5. 前台功能需求

### 5.1 进入空间

- 用户输入空间密钥进入活动列表。
- 用户可通过创建密码创建新空间。
- 分享链接可携带加密空间信息，打开后直达活动详情。
- 空间密钥不得在链接中明文暴露。

### 5.2 活动列表

列表展示当前空间下所有未取消活动，包含未来活动和过期活动。

活动卡片展示：

- 活动类型
- 活动标题
- 日期和时间
- 地点
- 费用
- 已报名人数 / 人数上限
- 活动状态：可报名、已满、已过期

列表支持按活动类型筛选：全部、羽毛球、篮球、剧本杀、桌游、其他。

### 5.3 活动详情

详情页展示：

- 活动类型和标题
- 公共信息：时间、地点、费用、人数、发布人
- 类型专属信息：由活动类型模板决定
- 报名名单
- 报名入口
- 分享链接入口
- 修改活动入口
- 取消活动入口

报名入口在活动满员、过期或取消后不可用。

### 5.4 发布活动

发布流程分两步：

1. 选择活动类型
2. 填写活动信息

公共必填字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 活动类型 | enum | 是 | 由活动类型模板提供 |
| 标题 | text | 是 | 活动名称 |
| 日期 | date | 是 | 活动日期 |
| 开始时间 | time | 是 | 活动开始时间 |
| 结束时间 | time | 是 | 活动结束时间 |
| 地点 | text | 是 | 活动地点 |
| 费用 | number/text | 是 | 可为 0、AA 或固定金额 |
| 人数上限 | number | 是 | 满员后不可报名 |
| 发布人 | text | 是 | 自由填写 |
| 活动取消码 | text | 是 | 用于修改或取消活动 |
| 备注 | text | 否 | 补充说明 |

类型专属字段由模板定义。

### 5.5 默认活动类型

#### 羽毛球

- 场地数量
- 水平要求
- 装备说明

#### 篮球

- 半场 / 全场
- 队伍人数
- 分队方式
- 水平要求

#### 剧本杀

- 剧本名称
- 剧本类型
- 所需人数
- 预计时长
- 是否已有 DM

#### 桌游

- 游戏清单
- 是否教学
- 适合人数
- 是否自带游戏

#### 其他

- 自定义说明

### 5.6 报名与取消报名

报名字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 姓名 / 昵称 | text | 是 | 报名名单展示 |
| 取消码 | text | 是 | 取消报名时验证 |

规则：

- 满员后不可报名。
- 过期活动不可报名。
- 同一姓名是否允许重复报名由系统配置决定，默认允许。
- 取消报名必须输入该报名记录的取消码。

### 5.7 分享能力

- 每个活动生成独立分享链接。
- 分享链接加密携带空间信息和活动 ID。
- 他人打开后自动进入空间并直达活动详情。
- 提供复制链接操作。

## 6. 后台功能需求

### 6.1 空间列表

展示所有空间：

- 空间名称或标识
- 常用活动类型
- 活动数量
- 最近活动时间
- 状态

### 6.2 空间详情

展示单个空间：

- 空间基础信息
- 未来活动数量
- 报名人数统计
- 活动类型分布
- 最近活动列表

### 6.3 活动列表

跨空间查看活动，支持运维处理：

- 按空间筛选
- 按活动类型筛选
- 按状态筛选
- 查看报名人数
- 查看异常活动

### 6.4 活动类型管理

管理员可以维护活动类型模板：

- 新增活动类型
- 编辑类型名称和图标
- 配置专属字段
- 配置字段是否必填
- 配置字段展示位置
- 启用或停用类型

## 7. 非功能需求

- 支持桌面网页端和手机网页端。
- 优先适配微信内置浏览器。
- 无需安装 App。
- 数据按空间隔离。
- 密钥和取消码只保存哈希，不保存明文。
- 分享链接不得明文暴露空间密钥。
- 前端使用原生 HTML / CSS / JS。
- 后端使用 PocketBase 和 SQLite。

## 8. 页面清单

### 前台

| 页面 | 说明 |
|---|---|
| 首页 - 进入空间 | 输入密钥进入空间，展示多活动能力 |
| 活动列表 | 展示空间内所有活动，支持类型筛选 |
| 活动详情 | 展示活动信息、类型字段、报名名单和报名入口 |
| 发布活动 - 选择类型 | 选择羽毛球、篮球、剧本杀、桌游或其他 |
| 发布活动 - 填写信息 | 填写公共字段和类型专属字段 |

每个页面需要桌面版和手机版设计。

### 后台

| 页面 | 说明 |
|---|---|
| 登录 | 管理员登录 |
| 空间列表 | 管理所有空间 |
| 空间详情 | 查看单个空间的数据和活动 |
| 活动类型管理 | 管理类型模板和字段配置 |
| 活动列表 | 跨空间查看和筛选活动 |

每个页面需要桌面版和手机版设计。

## 9. 暂不做范围

- 公开活动广场
- 用户账号体系
- 在线支付
- 消息通知
- 活动评论区
- 地图搜索
- 推荐系统

# 羽毛球场次预约系统 — 需求文档

## 1. 产品定位

供熟人圈子使用的轻量级羽毛球场次共享与报名系统。解决微信群聊天中场次信息被覆盖、难以汇总的问题。信任制设计，无需注册登录。

---

## 2. 核心概念

### 空间（租户）
- 每个"圈子"拥有一个独立空间，通过**密钥**标识
- 不同密钥对应不同空间，数据完全隔离
- 持有密钥 = 持有该空间的完整操作权限

### 密钥
- 由用户自定义的字符串（如 `badminton-groupA`）
- 在链接中以加密形式传递，不以明文暴露
- 持有分享链接等同于持有密钥

### 取消码
- 创建场次或报名时由操作者自行设定的一段字符串
- 取消操作时需输入相同的取消码才能执行
- 轻量级身份验证，朋友圈信任制使用

---

## 3. 用户角色

单一角色：**持有密钥的成员**。无管理员/普通用户之分，所有人权限相同（取消码机制做轻量保护）。

系统维护者（即部署者）通过服务器配置管理创建密码，不体现在产品 UI 中。

---

## 4. 功能需求

### 4.1 空间管理

| 功能 | 说明 |
|------|------|
| 创建空间 | 输入「创建密码」+ 自定义密钥，成功后进入该空间 |
| 进入空间 | 输入已有密钥，密钥存在则进入，不存在则提示错误 |
| 持久访问 | 进入空间后，密钥保存在浏览器本地，下次访问自动进入 |

**创建密码管理（运维层面，无 UI）：**
- 创建密码存储在服务器配置文件 / 环境变量中
- 部署者通过 SSH 登录服务器手动修改，支持定期更新
- 更新后旧密码立即失效，已有空间不受影响

### 4.2 场次管理

**发布场次**

填写字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 日期 | 日期 | ✓ | 打球日期 |
| 开始时间 | 时间 | ✓ | 如 19:00 |
| 结束时间 | 时间 | ✓ | 如 21:00 |
| 场地名称 | 文本 | ✓ | 如"XX 体育馆 3 号馆" |
| 场地数量 | 数字 | ✓ | 几片场地 |
| 每人费用 | 数字 | ✓ | 元 |
| 人数上限 | 数字 | ✓ | 超出后不可报名 |
| 发布人姓名 | 文本 | ✓ | 自由填写 |
| 取消码 | 文本 | ✓ | 用于修改/取消场次的验证 |
| 备注 | 文本 | — | 可选 |

**修改场次**
- 任何人均可尝试修改
- 在场次详情页内展开编辑表单（不跳转新页面）
- 需输入该场次的取消码验证通过后方可修改
- 过期场次不可修改

**取消场次**
- 任何人均可尝试取消
- 需输入该场次的取消码验证通过后方可取消
- 取消后场次从列表消失，已报名数据一并删除

### 4.3 报名管理

**报名**

填写字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 姓名/昵称 | 文本 | ✓ | 自由填写 |
| 取消码 | 文本 | ✓ | 用于取消本条报名的验证 |

- 报名人数达到上限后，报名入口关闭
- 同一场次同一姓名可重复报名（不做限制，靠道德约束）

**取消报名**
- 报名列表中每条记录有取消入口
- 需输入该条报名的取消码验证通过后方可取消

### 4.4 场次列表

- 展示当前空间内所有**未取消**的场次（包含过期场次）
- 按日期升序排列，过期场次置于列表底部或视觉上做区分（如灰色）
- 每条场次卡片展示：日期、时间、场地、费用、已报名人数/上限
- 点击进入场次详情页

### 4.5 场次详情页

展示内容：
- 场次完整信息
- 报名名单（姓名 + 报名时间）
- 报名入口（未满且未过期时显示，过期后隐藏或置灰）
- 修改场次：详情页内展开编辑表单，过期场次不显示修改入口
- 取消场次入口（过期场次不显示）
- 分享链接按钮

### 4.6 分享能力

- 每个场次生成独立分享链接
- 链接中加密携带：空间密钥 + 场次 ID
- 他人点开链接：自动进入对应空间，直达该场次详情页，无需手动输入密钥
- 提供"复制链接"按钮，方便粘贴到微信群

---

## 5. 非功能需求

| 项目 | 要求 |
|------|------|
| 响应式设计 | 同时支持桌面端和移动端，布局自适应，不出现错位或操作不便的情况 |
| 移动端优先 | 微信内置浏览器友好，按钮足够大，表单易于手机输入 |
| 无需安装 | 纯网页，无 App |
| 通知 | 不需要，用户自行分享链接到群 |
| 数据隔离 | 不同密钥空间数据严格隔离 |
| 密钥安全 | 链接中密钥加密传输，不明文暴露 |
| 空间入口 | 每次进入均需手动输入密钥，不在本地存储 |

---

## 6. 技术栈（已确定）

| 层级 | 技术 |
|------|------|
| 前端 | 原生 HTML / CSS / JS（无框架） |
| 后端 | PocketBase（单二进制，内置 SQLite + REST API） |
| 部署 | 单机，Nginx 反向代理（可选） |

---

## 7. 数据结构（草案）

### spaces（空间）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 自动生成 |
| key_hash | string | 密钥的哈希值（不存明文） |
| created | datetime | 创建时间 |

### sessions（场次）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 自动生成 |
| space | relation | 关联 spaces |
| date | date | 打球日期 |
| start_time | string | 开始时间 |
| end_time | string | 结束时间 |
| venue | string | 场地名称 |
| court_count | number | 场地数量 |
| fee | number | 每人费用（元） |
| max_players | number | 人数上限 |
| organizer | string | 发布人姓名 |
| cancel_code_hash | string | 取消码的哈希值（不存明文） |
| note | text | 备注 |
| status | string | active / cancelled |
| created | datetime | 自动生成 |

### signups（报名）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 自动生成 |
| session | relation | 关联 sessions |
| name | string | 报名者姓名/昵称 |
| cancel_code_hash | string | 取消码的哈希值（不存明文） |
| created | datetime | 报名时间 |

---

## 8. 页面清单

| 页面 | 路径 | 说明 |
|------|------|------|
| 入口页 | `/` | 输入密钥进入空间，或创建新空间 |
| 场次列表 | `/space` | 当前空间的所有场次（含过期） |
| 场次详情 | `/session?id=xxx` | 详情、报名名单、报名/取消/修改（展开式）操作 |
| 发布场次 | `/new` | 填写表单发布新场次 |

---

## 9. 超出范围（本版本不做）

- 用户账号系统
- 消息通知（推送/短信/邮件）
- 历史场次统计
- 管理后台 UI（直接用 PocketBase 自带后台）
- 场次评论/讨论
